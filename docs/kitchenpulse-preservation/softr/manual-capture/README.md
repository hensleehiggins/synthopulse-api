# Final Manual Capture Workspace

This folder contains only artifacts the read-only MCP could not preserve. Do not re-capture application/page/block/data-source metadata already stored in `../mcp-export/`.

Paste code/configuration into the exact placeholder files; update the paired metadata/checklist status only after verifying the saved content. Never place credentials, cookies, OAuth tokens, or the Billing secret in this repository.

