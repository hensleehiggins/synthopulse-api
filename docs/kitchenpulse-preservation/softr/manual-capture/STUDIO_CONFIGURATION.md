# Studio-only configuration capture

Completion status: **[ ] NOT CAPTURED**. Copy text/configuration into this file wherever possible; screenshots are named only for visual state that cannot be copied faithfully.

## Theme and assets

Capture theme name/version, color tokens, typography, spacing/radius/shadow settings, custom fonts, logos/icons/images, and each asset’s source/export location. Screenshot: `screenshots/studio-theme-and-assets.png`.

## Navigation, header, and sidebar

Capture each menu/header/sidebar item: label, order, route/URL, icon, target behavior, visibility/user-group/device rules, and any conditional logic. Screenshot: `screenshots/studio-navigation-header-sidebar.png`.

## Authentication, login, and signup

Capture enabled providers, authentication mode, login/signup page mapping, form fields, allowed domains, post-login/signup destinations, password/reset behavior, and user-group assignment rules. Screenshot: `screenshots/studio-auth-login-signup.png`.

## Redirects and custom domain

Copy each redirect source, destination, condition, and status code. Record custom domain, subdomain, DNS/SSL status, and default-domain behavior. Screenshots: `screenshots/studio-redirects.png` and `screenshots/studio-custom-domain.png`.

## Global/app/page custom code

Copy full global CSS, global JavaScript, app-level custom code, and every non-empty page-level custom-code payload as text. For each page-level payload, prefix it with page name, ID, and route. Do not use screenshots when text can be copied. Do not include credentials.

## Access redirections

Capture all four mappings: triggering user group/condition, destination page/URL, priority/order, and fallback behavior. Screenshot: `screenshots/studio-access-redirections.png`.

## Published state

Record app published state and, for each page whose enabled/published state differs from the MCP listing, page ID, route, observed state, and timestamp. Screenshot: `screenshots/studio-publish-state.png`.

